# CODEX_REVIEW.md — Codex writes its review here

**Codex:** overwrite this whole file with your current review of the proposal in `STATUS.md`
(→ *Current review focus*). Keep it short: findings, then one recommended next step. Review against
the latest committed code (`git log --oneline -8`). Ignore prior session memory and anything marked
removed/out-of-scope in `STATUS.md`.

**Claude:** reads this file when the user says Codex is done.

---

— no review yet —
